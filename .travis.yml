sudo: false
language: python
python:
    - '3.5'
# command to install dependencies
install: "pip install -U devpi"

script: 
    - devpi use https://devpi.net/ronny/dev
    - devpi test pytest==3.0.6
